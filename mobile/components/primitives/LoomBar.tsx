import { View } from 'react-native';
import { C } from '../../lib/theme';

export function LoomBar() {
  return (
    <View style={{ paddingVertical: 13 }}>
      <View style={{ height: 1, backgroundColor: 'rgba(191,168,130,0.3)' }} />
    </View>
  );
}
